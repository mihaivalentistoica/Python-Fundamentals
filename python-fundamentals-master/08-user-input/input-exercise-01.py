country_capital_dict = {}

# a. Use input function to query user for a country and its capital three times for each pair (6 inputs total).
country = input("Enter country name: ")
# a. Use input function to query user for a country and its capital three times for each pair (6 inputs total).
capital = input(f"Enter {country} capital name: ")

# Add country and its capital to the dictionary
country_capital_dict[country] = capital

# a. Use input function to query user for a country and its capital three times for each pair (6 inputs total).
country = input("Enter country name: ")
# a. Use input function to query user for a country and its capital three times for each pair (6 inputs total).
capital = input(f"Enter {country} capital name: ")

# Add country and its capital to the dictionary
country_capital_dict[country] = capital

# a. Use input function to query user for a country and its capital three times for each pair (6 inputs total).
country = input("Enter country name: ")
# a. Use input function to query user for a country and its capital three times for each pair (6 inputs total).
capital = input(f"Enter {country} capital name: ")

# Add country and its capital to the dictionary
country_capital_dict[country] = capital

# b. Print out the resulting dictionary.
print(country_capital_dict)
